<!doctype html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Elegir duración de sesión</title>
  <style>
    .modal { background:#fff;border-radius:12px;padding:20px;max-width:420px;margin:80px auto;text-align:center;font-family:system-ui;box-shadow:0 6px 24px rgba(0,0,0,.12); }
    select,button { padding:10px; margin-top:12px; }
  </style>
</head>
<body>
  <div class="modal">
    <h2>Bienvenido</h2>
    <p>Deseas mantener la sesión activa por cuanto tiempo?</p>
    <select id="choice">
      <option value="1day">1 día</option>
      <option value="60days">60 días</option>
      <option value="always">Siempre</option>
    </select>
    <br/>
    <button id="save">Guardar</button>
  </div>

  <script>
    document.getElementById('save').addEventListener('click', async () => {
      const choice = document.getElementById('choice').value;
      const res = await fetch('/auth/session-duration', {
        method:'POST',
        headers:{'Content-Type':'application/json'},
        body: JSON.stringify({ choice })
      });
      const j = await res.json();
      if (j.ok) {
        alert('Sesión guardada. Serás redirigido a tus notas.');
        location.href = '/notes';
      } else {
        alert('Error al guardar la sesión');
      }
    });
  </script>
</body>
</html>
